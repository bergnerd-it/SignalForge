# SignalForge M1b corrective closeout

**Date:** 2026-09-12  
**Reviewed base commit:** `6d434c880bd9d316c3cb6fdb50031b7488ca2026` (`implement m1a close`)  
**Corrected state:** the same commit plus the complete uncommitted M1b working tree, including tracked and untracked files  
**Status:** M1b native implementation and recovery gates PASS; Docker/container deployment remains NOT VERIFIED

## Scope and source identity

The independent review covered an uncommitted M1b candidate on commit `6d434c880bd9d316c3cb6fdb50031b7488ca2026`. This repair retained that candidate and unrelated working-tree changes. No commit was created. At final inspection `git status --short` contained 55 entries; the tracked diff contained 1,239 additions and 534 deletions, with the M1b implementation also containing untracked source, migration, test, report and fixture files. Reviewers must therefore inspect `git diff`, `git diff --cached` and untracked files rather than commit-to-commit history alone.

All database execution used JUnit temporary directories or `/tmp/signalforge-m1b-fixes.LZX9JL`. The configured owner database was never opened, copied, migrated or queried. The preserved old binary `.checkpoint/m1a-checkpoint.jar` was not modified.

The prior report [`completion-report-M1b.md`](completion-report-M1b.md) remains as historical evidence and is now marked **SUPERSEDED**. This report replaces its readiness conclusion.

## Findings 1–13

| # | Review finding and disposition | Correction | Regression evidence |
|---|---|---|---|
| 1 | **Verified defect fixed:** production foreign keys rejected research creation because the request referenced a portfolio that did not yet exist. | `OperationService.createPortfolio` now validates and hashes the normalized request, inserts the portfolio before `portfolio_creation_requests`, and keeps portfolio, request, opening operation, ledger and state in one transaction ([`OperationService.java:101`](../../backend/src/main/java/com/bergnerd/signalforge/app/operation/OperationService.java#L101), [`OperationService.java:158`](../../backend/src/main/java/com/bergnerd/signalforge/app/operation/OperationService.java#L158)). Pool connections enable both foreign keys and a bounded busy timeout in [`application.yml`](../../backend/src/main/resources/application.yml). | Production-configured controller creation and per-connection PRAGMA checks pass in [`SignalForgeIntegrationTest.java:73`](../../backend/src/test/java/com/bergnerd/signalforge/app/SignalForgeIntegrationTest.java#L73) and `:88`. Native fresh and upgraded packaged applications both returned HTTP 201 for PAPER/EUR creation. |
| 2 | **Verified defect fixed:** Angular manual trades omitted the required retry key. | The frontend creates one `trade-<UUID>` per observable/user intent and sends `Idempotency-Key` ([`portfolio.service.ts:28`](../../frontend/src/app/services/portfolio.service.ts#L28)); the controller rejects a missing key ([`PortfolioController.java:22`](../../backend/src/main/java/com/bergnerd/signalforge/app/portfolio/PortfolioController.java#L22)). | Angular HTTP contract test, backend exact-payload test, missing-key HTTP 400, conflicting-key HTTP 409 and same-key replay pass ([`SignalForgeIntegrationTest.java:132`](../../backend/src/test/java/com/bergnerd/signalforge/app/SignalForgeIntegrationTest.java#L132)). Native browser BUY produced one execution and updated cash/position. |
| 3 | **Verified defect fixed:** chat had no durable request/plan lifecycle. | V2 adds `chat_requests` plus stable indexed action identities ([`V2__m1b_review_fixes.sql:3`](../../backend/src/main/resources/db/migration/V2__m1b_review_fixes.sql#L3)). `ChatService` checks hash/state before model access, leases inference and action execution, persists the complete immutable validated plan, resumes pending actions with stable keys, and stores the final response outside deletable chat text ([`ChatService.java:73`](../../backend/src/main/java/com/bergnerd/signalforge/app/chat/ChatService.java#L73), `:131`, `:153`, `:163`, `:184`, `:269`). The Angular client supplies `Idempotency-Key` ([`chat.service.ts:98`](../../frontend/src/app/services/chat.service.ts#L98)). | The real service tests cover completed replay/model-once, payload conflict, assistant persistence failure after trade, restart-style service recreation, partial trade/watchlist recovery, malformed later action, clear-and-replay, concurrent inference and concurrent action workers ([`ChatRecoveryIntegrationTest.java:92`](../../backend/src/test/java/com/bergnerd/signalforge/app/chat/ChatRecoveryIntegrationTest.java#L92), `:122`, `:144`, `:169`, `:187`, `:204`, `:235`). |
| 4 | **Verified defect fixed:** completed trade retries fetched a new quote first. | Portfolio scope and quantity are normalized, then `findCompletedTrade` runs before market access ([`PortfolioService.java:121`](../../backend/src/main/java/com/bergnerd/signalforge/app/portfolio/PortfolioService.java#L121), [`OperationService.java:240`](../../backend/src/main/java/com/bergnerd/signalforge/app/operation/OperationService.java#L240)). A replay returns the stored execution without a live portfolio valuation. | [`PortfolioServiceTest.java:175`](../../backend/src/test/java/com/bergnerd/signalforge/app/PortfolioServiceTest.java#L175) proves the original ID/fill is returned after the market mock is reset and verifies zero provider interactions. Conflict normalization remains covered by operation tests. |
| 5 | **Verified defect fixed:** research execution fabricated instrument identity. | PAPER execution now requires a pre-existing `RESOLVED` symbol/currency listing and otherwise rejects before an operation write ([`OperationService.java:578`](../../backend/src/main/java/com/bergnerd/signalforge/app/operation/OperationService.java#L578)). V2 quarantines only candidate-generated ID/provenance/hard-coded-ISIN combinations as `UNVERIFIED_CANDIDATE`, leaving a trusted instrument with the same ISIN untouched ([`V2__m1b_review_fixes.sql:33`](../../backend/src/main/resources/db/migration/V2__m1b_review_fixes.sql#L33)). | Unknown-listing/no-write test passes at [`OperationServiceIntegrationTest.java:595`](../../backend/src/test/java/com/bergnerd/signalforge/app/operation/OperationServiceIntegrationTest.java#L595). Candidate upgrade/quarantine and genuine-same-ISIN preservation pass at [`MigrationRecoveryIntegrationTest.java:444`](../../backend/src/test/java/com/bergnerd/signalforge/app/db/MigrationRecoveryIntegrationTest.java#L444). |
| 6 | **Verified defect fixed:** empty or structurally incomplete version history was accepted. | `MigrationRunner` supports exact versions 1–2, rejects unknown/missing/gapped history and validates required tables, columns, unique keys, composite ledger FK, financial triggers and immutability triggers before readiness ([`MigrationRunner.java:213`](../../backend/src/main/java/com/bergnerd/signalforge/app/db/migration/MigrationRunner.java#L213), `:360`, `:398`). | Empty history and forged checksums with missing schema fail at [`MigrationRecoveryIntegrationTest.java:402`](../../backend/src/test/java/com/bergnerd/signalforge/app/db/MigrationRecoveryIntegrationTest.java#L402) and `:418`; malformed and newer versions remain covered. |
| 7 | **Previously untested risk now verified:** migration rollback, historical checksum behavior and old-binary restore. | Applied V1 rows remain unchanged. Recognized candidate V1 checksum metadata receives follow-on V2 only. Current V1 checksum covers V1 SQL, the compiled converter, compiled accounting dependency and immutable policy resource ([`LegacyDataMigrator.java:31`](../../backend/src/main/java/com/bergnerd/signalforge/app/db/migration/LegacyDataMigrator.java#L31), [`MigrationRunner.java:333`](../../backend/src/main/java/com/bergnerd/signalforge/app/db/migration/MigrationRunner.java#L333)). A conversion checkpoint permits real transactional failure injection. | Candidate transition `:444`, real rename/write rollback and successful retry `:494`, digest mutation `:521`, and restored-copy startup of the unchanged checkpoint JAR `:301` all pass in [`MigrationRecoveryIntegrationTest.java`](../../backend/src/test/java/com/bergnerd/signalforge/app/db/MigrationRecoveryIntegrationTest.java). The old binary's known snapshot/empty-watchlist compatibility behavior is distinguished from backup integrity. |
| 8 | **Previously untested risk now verified and bounded:** process-local locks alone did not prove SQLite writer behavior. | Each operation rejects a surrounding transaction, holds its service lock through `TransactionTemplate.execute` commit/rollback, acquires a database write before mutable reads, and retries the whole same-key operation at most four times for SQLite busy conflicts ([`OperationService.java:316`](../../backend/src/main/java/com/bergnerd/signalforge/app/operation/OperationService.java#L316), `:608`, `:614`). Controlled seams cover cash, position, execution, ledger and operation-result writes (`:416`, `:439`, `:450`, `:463`, `:484`). | Independent service instances/pool connections prove concurrent creation, distinct-key overspend, same-key execution identity, oversell prevention and bounded busy exhaustion followed by same-key recovery ([`OperationServiceIntegrationTest.java:287`](../../backend/src/test/java/com/bergnerd/signalforge/app/operation/OperationServiceIntegrationTest.java#L287), `:493`, `:531`, `:566`, `:613`). Real failure-point rollback and retry pass at `:119`. |
| 9 | **Verified defect fixed:** unsupported precision, inconsistent creation hashes and false zero reconciliation. | `AccountingCore` defines policy `v2-decimal-grammar-half-even`, scales and finite bounds; it rejects unsupported client precision and normalizes accepted values before hashing ([`AccountingCore.java:18`](../../backend/src/main/java/com/bergnerd/signalforge/app/accounting/AccountingCore.java#L18), `:72`). V2 adds TEXT grammar/scale/length and enum triggers without REAL casts ([`V2__m1b_review_fixes.sql:52`](../../backend/src/main/resources/db/migration/V2__m1b_review_fixes.sql#L52)). Migration audit differences are actual converted-minus-original values for cash and basis ([`LegacyDataMigrator.java:119`](../../backend/src/main/java/com/bergnerd/signalforge/app/db/migration/LegacyDataMigrator.java#L119), `:189`). | Accounting precision/bound tests pass. Migration fixtures prove `1.005 -> 1.00` gives `-0.005`, `1.015 -> 1.02` gives `+0.005`, and recorded basis differences equal converted minus raw ([`MigrationRecoveryIntegrationTest.java:224`](../../backend/src/test/java/com/bergnerd/signalforge/app/db/MigrationRecoveryIntegrationTest.java#L224)). |
| 10 | **Verified defect fixed:** research endpoints accepted the wrong mode/currency and ID-only detail reads. | Creation requires exact PAPER/EUR; detail lookup includes ID, server-owned `default`, and PAPER mode ([`ResearchPortfolioController.java:42`](../../backend/src/main/java/com/bergnerd/signalforge/app/research/ResearchPortfolioController.java#L42), `:108`). | Production-configured API tests reject LEGACY_DEMO and return 404 for both legacy and another owner's PAPER account ([`SignalForgeIntegrationTest.java:104`](../../backend/src/test/java/com/bergnerd/signalforge/app/SignalForgeIntegrationTest.java#L104)). |
| 11 | **Verified defect fixed:** stale research selection and component subscription lifecycle. | `ResearchService` drives selection through `Subject` plus `switchMap`, cancelling obsolete HTTP requests ([`research.service.ts:26`](../../frontend/src/app/services/research.service.ts#L26)). `ResearchComponent` uses `takeUntilDestroyed` for all long-lived subscriptions and explicitly schedules zoneless rendering ([`research.component.ts:36`](../../frontend/src/app/components/research/research.component.ts#L36)). | Angular test proves A request cancellation when B is selected ([`services.spec.ts:274`](../../frontend/src/app/services/services.spec.ts#L274)). Native browser testing exposed and then verified the zoneless loading-state correction: the empty state rendered, portfolio creation completed, and detail showed EUR `1234.56`. |
| 12 | **Verified defect fixed:** the live UI fabricated price 100, equity 10000 and purchase-cost valuations. | Selected tick and equity are nullable; trade controls display `UNAVAILABLE` and disable submission without an executable tick ([`app.ts:151`](../../frontend/src/app/app.ts#L151), [`app.html:42`](../../frontend/src/app/app.html#L42), [`trade-bar.component.ts:213`](../../frontend/src/app/components/trade-bar/trade-bar.component.ts#L213)). Backend/TypeScript portfolio valuation fields are nullable; a missing quote no longer falls back to average cost ([`PortfolioService.java:49`](../../backend/src/main/java/com/bergnerd/signalforge/app/portfolio/PortfolioService.java#L49)). Position, header, P&L and heatmap views render or omit unavailable values explicitly. | Missing/loaded component states pass; [`PortfolioServiceTest.java:193`](../../backend/src/test/java/com/bergnerd/signalforge/app/PortfolioServiceTest.java#L193) proves purchase cost is not returned as market value. Native browser trade remained enabled only after simulator ticks arrived. |
| 13 | **Documentation/toolchain defect fixed:** the old report overstated readiness and the host toolchain was unpinned. | Added `.nvmrc` and `.node-version` with `24.21.0`, plus exact compatible engines/package manager in [`package.json:12`](../../frontend/package.json#L12). The old COMPLETE status is visibly superseded. Empty `demo`/`research` routes now make pushed view URLs reloadable without router error ([`app.routes.ts`](../../frontend/src/app/app.routes.ts)). | Clean install, tests and build pass with Node 24.21.0/npm 11.18.0. Node 26.8.1 still exited 134 during `ng build`; this does not establish Node 26 incompatibility and was not investigated further. |

## Required evidence matrix

| Evidence | Actual result |
|---|---|
| Production-configured fresh API | PASS: all sampled pool connections reported `foreign_keys=1`, `busy_timeout=5000`; PAPER/EUR create returned 201. |
| Native packaged fresh database | PASS on port 18081: health 200, PAPER/EUR create 201, manual AAPL trade 200, same-key replay 200 with identical execution ID and stored fill. |
| Native packaged legacy upgrade | PASS on port 18082: pre-migration backup created, three-owner conversion completed, health 200, legacy portfolio 200, PAPER/EUR create 201 and manual trade 200. |
| Native browser | PASS on disposable fresh DB: research empty state rendered, Browser Paper created with exact EUR `1234.56`, detail showed `UNAVAILABLE` valuation, and demo BUY 1 AAPL updated cash and one holding. The test found and fixed missing zoneless change detection; pushed `/research` and `/demo` paths are now recognized routes. |
| Chat recovery stages | PASS: completed replay, conflict, assistant-message failure after committed trade, recreated service recovery, partial trade/watchlist resume, malformed-plan prevalidation, clear/replay, inference lease and action-worker lease. |
| Writer races | PASS: independent service instances for creation, same-key trade and distinct-key account contention; exactly one same-key operation ID; no overspend or oversell. |
| Atomicity seams | PASS for cash, position, execution, ledger and operation-result failures through real `OperationService`; every failure rolled back fragments and the same key subsequently succeeded. |
| Busy exhaustion | PASS with a real held SQLite write transaction and a second FK-enabled pool using a 1 ms busy timeout; four attempts exhausted under two seconds, then the same key succeeded once after rollback. |
| Schema rejection | PASS for empty history, missing V1, unknown/newer version, checksum mismatch, malformed schema and forged valid-looking V1/V2 history with missing structures. |
| Candidate compatibility | PASS: exact prior candidate V1 checksum recognized, V1 metadata preserved, V2 appended, generated identity quarantined, trusted same-ISIN identity retained. |
| Conversion rollback | PASS: injected failure after real legacy writes left legacy names/data and no migration history; rerun from that state completed. |
| Digest mutation | PASS: changed compiled migrator artifact changes the conversion digest. |
| Old-binary restore | PASS: backup copied to a distinct DB, schema/data compared, original M1a checkpoint JAR launched with safe explicit DB/port, bounded health readiness reached, stable user/cash/empty-watchlist fields verified, process stopped. |

## Commands and results

| Command | Toolchain | Result |
|---|---|---|
| `backlog instructions overview` | Backlog.md CLI | PASS; executed before implementation. TASK-11 tracks the repair. |
| `./gradlew spotlessApply` | Gradle 8.10.2 / Java 21 | NOT AVAILABLE: the backend has no `spotlessApply` task. No formatting success is claimed. |
| `JAVA_HOME=.../jdk-21.0.12.1+1/Contents/Home ./gradlew clean test` | Eclipse Adoptium 21.0.12.1 | PASS: 104 tests, 0 failures, 0 errors, 0 skipped. |
| `JAVA_HOME=... ./gradlew build -x test` | Eclipse Adoptium 21.0.12.1 | PASS; executable Boot JAR and plain JAR built. |
| `npx --yes --package=node@24.21.0 --package=npm@11.18.0 npm ci` | Node 24.21.0 / npm 11.18.0 | PASS: 375 packages installed, 0 vulnerabilities. |
| `... npm run test -- --watch=false` | Node 24.21.0 / npm 11.18.0 | PASS: 3 files, 23 tests. |
| `... npm run build` | Node 24.21.0 / npm 11.18.0 | PASS: production bundle generated. Warning: `research.component.css` is 1.20 kB above its 4.00 kB component-style budget. |
| `npm run build` using host defaults | Node 26.8.1 / npm 11.19.0 | FAIL: exit 134 during Angular build, without an Angular diagnostic. Superseded by the pinned successful build. |
| `git diff --check` | Git | PASS; only configured LF-to-CRLF working-copy warnings. |
| Packaged native HTTP commands (`java -jar`, `curl`) | Java 21 / disposable fresh and fixture-copy SQLite files | PASS for health, creation, manual trade and same-key replay/upgrade flows described above. |
| Angular dev server plus in-app browser | Node 24.21.0 / npm 11.18.0 / disposable DB | PASS for the research creation and manual trade walkthrough. |

The full backend test total includes focused unit, controller and integration tests. The restore test starts an actual old binary, so its passing name is backed by process readiness and database assertions rather than an artifact hash alone.

## Compatibility, limitations and readiness

V1 remains an immutable historical SQL migration. The candidate compatibility path accepts only the exact prior candidate checksum derived from the reviewed V1 SQL and its old conversion marker; it does not accept arbitrary checksums and does not rewrite the applied row. V2 records the fixes and quarantines only rows matching the reviewed generator's combined ID, provenance and ISIN evidence.

The browser and native checks use the built-in synthetic market simulator. Historical import/data inspection remains M2, and the backtest engine remains M3. No M2 work was implemented.

Docker/container execution was not repeated because the prompt preserves the prior registry-access deferral. It remains **NOT VERIFIED** as a deployment gate. The successful native browser flow does verify the application behavior that was feasible without a registry. The CSS budget warning is a non-blocking build warning and should be reduced in a later UI cleanup.

With all 13 review findings corrected and the required native accounting, migration, concurrency, idempotency, recovery, API and browser paths passing, the M1b corrected working tree is ready for review and commit. M2 should begin only from the reviewed/committed form of this complete working tree so none of the currently untracked migration or test files are omitted.
