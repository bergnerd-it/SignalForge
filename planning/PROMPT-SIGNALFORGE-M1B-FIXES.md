# SignalForge — M1b corrections after independent review

Implement the corrections required by **all 13 findings** in `codereview-M1b.md`. Complete M1b before starting M2. Preserve the useful existing accounting implementation and unrelated working-tree changes. This is a repair-and-verification task, not another report-only review.

## 1. Read the actual state and correct the readiness baseline

Read applicable `AGENTS.md` and task-tracking instructions, the original `planning/PROMPT-SIGNALFORGE-M1B.md`, the v0.2 addendum, and both supplied reports:

- `completion-report-M1b.md` (the repository may store it as `planning/reports/research-M1b.md`).
- `codereview-M1b.md` (use its actual supplied/repository location).

Locate reports by filename/content if necessary; do not mistake an older milestone report for either one. Follow the existing task tracker. Record current HEAD and the complete reviewed/modified diff, including untracked implementation files. The review covered uncommitted M1b work on `6d434c880bd9d316c3cb6fdb50031b7488ca2026`; verify each finding against the present code rather than assuming those line numbers remain current.

Treat M1b as incomplete pending these repairs. Preserve the original reports as dated evidence and visibly mark the old COMPLETE status as superseded by the review. Passing 79 backend and 21 frontend tests proves those tests ran; it does not prove the missing production paths or durability scenarios.

Retain the agreed deferral of Docker/container browser checks when registry access is blocked. Native application flows, accounting, migrations, concurrency and old-binary recovery remain required. Do not spend the task repeatedly retrying an unchanged registry outage. Do not disable TLS checks, foreign keys, request-origin enforcement or failed tests.

All execution uses disposable databases/volumes, synthetic quotes and mock LLMs. Do not open or migrate the owner's working database. Preserve a reproducible candidate checkpoint and backups of disposable candidate schemas before schema changes. Do not push, merge, publish, or begin M2.

## 2. Restore real user flows — findings 1, 2, 5 and 10

### Creation and production-equivalent SQLite

First reproduce the failed research creation through the real application/controller and configured datasource. Make the test assert `PRAGMA foreign_keys=1` on every borrowed connection. Use the actual migrations and a unique temporary file-backed SQLite database, not a reduced test schema or a raw datasource with weaker settings.

Fix the creation-request foreign-key ordering. One valid approach is inserting the portfolio before its referencing creation-request row within the same transaction. Preserve the owner/key uniqueness guarantee: a losing transaction must roll back its provisional portfolio and funding, then resolve the original request/result outside that failed transaction. Never swallow the conflict and commit an orphan account. An in-flight request may have a documented retryable state; it must not double-fund.

Use the server-owned local identity at research boundaries. Creation accepts only explicit PAPER/EUR. Detail lookup must include portfolio ID, server owner and PAPER mode; return 404 for out-of-scope IDs. Test another owner's account and a legacy account as negative cases.

Remove fabricated research identity from production code. Unknown/unresolved listings are rejected with no partial financial write. Trusted deterministic listings belong in test fixtures only. Never assign arbitrary symbols a copied ISIN, ETF type, XETRA venue, calendar or inception date. Keep unresolved legacy identities isolated.

Account for existing candidate rows generated by that faulty path: preserve their records/references and report or mark unsupported identity as unverified and non-executable through the candidate upgrade/validation policy. Do not silently treat them as trusted after removing the generator, and do not invalidate a genuine instrument solely because it shares the hard-coded ISIN. Base remediation on provenance and available evidence, using disposable candidate fixtures here.

### Manual-trade contract

Align the frontend, controller and application service on one documented stable-key contract, preferably `Idempotency-Key` as specified for research creation. Generate the key once per submitted user intent and reuse it for transport retries/ambiguous responses. A deliberate new order gets a new key. Reject missing keys explicitly; do not silently generate server UUIDs for retry-sensitive mutations.

Test the exact request emitted by the Angular client and send that shape through a real backend endpoint. Include positive execution, same-intent retry, missing-key rejection and conflicting-key reuse. Preserve existing useful response fields; document intentional request-contract changes.

## 3. Durable retry behavior — findings 3 and 4

### Trade replay before market access

Resolve portfolio scope and normalize client intent, then check a completed operation before fetching or validating a current quote. A completed matching retry must succeed even when the provider now throws or the latest quote is stale. Recheck uniqueness within the transaction before a new execution.

Test provider invocation count, changed market price and provider failure after the original commit. Assert the original operation/fill/result is returned and cash/positions/ledger are unchanged. Check canonical intent conflicts even on this fast path.

### Chat request and action lifecycle

Require a stable key from the built-in chat client and backend caller. Persist a canonical request hash, request/inference state, complete validated action set, stable per-action identity and durable result references outside deletable conversation text. Check request state before inserting a message with an already-used ID or invoking the model.

Persist and validate the whole action set before executing the first action, including supported watchlist mutations. Replay/resume uses that immutable set. Use stable per-action operation keys, transactionally recorded effects/results where possible, and resumable status updates where a financial operation already committed separately. Financial idempotency must cover a crash between trade commit and chat-action status persistence.

Define concurrent and restart behavior with bounded request ownership. Same-key concurrent requests must not run two action workers. Return the completed stored result, or a documented in-progress response with the same request identity. Once a validated plan exists, recovery must not ask the model for another plan. If a crash occurred before any plan was persisted, a fresh inference may be retried only after proving no actions could have executed for that request. No database transaction spans model inference.

Required tests call the real ChatService/controller with a controllable mock LLM:

- Matching completed retry invokes the model once and applies each action once.
- Conflicting reuse fails without actions.
- Assistant-text persistence fails after a committed trade; retry and application-context restart recover the stored result without another trade/model plan.
- Failure after one action in a multi-action plan resumes the remaining original actions, including watchlist actions.
- Two simultaneous requests with one key cannot execute duplicate actions.
- Malformed later actions cause validation failure before any action starts.
- Clearing conversation text preserves request/action/execution evidence and permits safe result replay.

Deleting manually seeded chat rows is not a substitute for these tests.

## 4. Transaction and concurrency evidence — finding 8

Exercise actual OperationService calls, with production-equivalent foreign-key-enabled pools and real transactions. Construct two service instances with separate Java locks and independent connections to the same disposable database. Use barriers/latches to produce real same-key and distinct-key contention; two SELECT statements do not establish writer correctness.

Implement bounded SQLite busy handling and whole-operation retry with the same key, including commit failures where applicable. Ensure writer coordination covers actual commit/rollback, and no hidden outer transaction postpones commit beyond the guard. Acquire write capability before reading balances to modify; do not nest manual BEGIN inside a Spring transaction.

Test concurrent account creation, same-key trades, distinct-key overspending and overselling, bounded busy exhaustion and retry after restart. Verify final row counts, operation/result IDs, cash, units and ledger reconciliation, not only return values.

Inject failures through controlled persistence seams/triggers reached by real operations at each required stage: ledger, cash, positions, executions and operation result. Verify all financial fragments roll back, then retry successfully with the same key. A generic TransactionTemplate test containing handwritten UPDATE statements does not verify OperationService atomicity.

## 5. Migration and recovery — findings 6 and 7

Reject unsupported version sets, missing/gapped history and a lone empty `schema_migrations` table. Verify the actual required tables, columns, foreign keys, uniqueness and ledger immutability triggers before declaring readiness. Test a forged valid-looking history row with missing structures as well as the empty-history case. Do not mark a database current simply because a history table exists.

Preserve applied migration history. Do not silently rewrite applied V1 definitions, erase checksums, or automatically delete an existing candidate database. Use a follow-on migration or an explicit compatibility transition for the recognized candidate schema. If the old checksum scheme needs transition, recognize specific prior metadata/schema and document its limited historical assurance; do not accept arbitrary checksum changes.

Replace the manually maintained conversion-version string with a reproducible digest covering the actual immutable conversion artifact and relevant conversion dependencies. Show that changing conversion logic changes the digest and causes the appropriate mismatch handling. Adding another constant is insufficient.

Inject a failure partway through a real legacy conversion, after relevant renames/writes. Compare pre/post schema and data and prove raw-table changes, converted state, ledger and migration metadata roll back together. Then rerun successfully from the same restored transaction state. Test fresh legacy upgrade and upgrade of a recognized existing M1b candidate schema as applicable.

Perform a real restore rehearsal: restore a backup into a different disposable file, compare schema/data, launch the preserved checkpoint JAR against that restored file with explicit safe configuration and port, wait for bounded readiness, verify stable fields and stop it. Preserve the original old binary. Document its known snapshot/empty-watchlist seeding effects separately from backup integrity. A JAR hash check and backup-table query alone are not an old-binary restore test.

## 6. Numeric policy and truthful reconciliation — finding 9

Define versioned decimal grammar, normalization, precision and finite bounds. Enforce them at every application/domain write entry. Reject unsupported client quantity/price precision before writing. Explicitly versioned server-side price quantization or cash HALF_EVEN booking is allowed where the contract calls for it; do not confuse that with silent alteration of unsupported input.

Normalize validated intent before hashing and storing. Equivalent accepted inputs must produce consistent hashes and results. For starting cash, either reject unsupported fractional cents or explicitly normalize once under the cash policy before hashing; use one documented rule throughout.

Keep exact string JDBC persistence and BigDecimal arithmetic. Add practical SQLite decimal-shape/type/bounds and enum constraints that the schema can enforce. Do not cast financial values to REAL for checking or aggregation. Application validation must remain exact even where a database constraint provides only a structural check. Validate corrupt persisted data before using it in financial calculations.

Calculate migration reconciliation differences from preserved original decimal representations and the actual converted values, using a declared sign convention. Include fixtures where rounding really changes values, such as cash `1.005` to `1.00` and `1.015` to `1.02` under HALF_EVEN. Under converted-minus-original, the differences are `-0.005` and `+0.005`; do not round these audit deltas to two decimals and hide them as zero. Verify position/basis reconciliation too.

Retain the independently specified accounting fixtures, split residual, full-liquidation and ledger replay tests from the original M1b prompt.

## 7. Frontend state and missing data — findings 11 and 12

Make selected-account loading cancel/discard obsolete responses, using the existing RxJS patterns such as selected-ID plus switchMap. Clean up component subscriptions through async bindings or lifecycle-aware teardown. Test a rapid A-to-B selection while A is pending, including cancellation/out-of-order completion behavior, and component destruction/recreation. Remove `any` from the new research contract tests.

Remove fallback price `100` and fallback portfolio equity `10000` from the live display path. Represent missing data explicitly and disable new trade submissions until an executable tick exists. A legitimate simulator tick is allowed when identified as synthetic demo data. Do not disable result replay for an already submitted intent merely because a fresh tick is now unavailable. Keep research state unaffected by demo SSE.

Test rendered missing/loaded states and the actual manual-trade request contract. Do not present an absent quote as zero or purchase cost as market value.

## 8. Consistent toolchain, evidence and report — finding 13

Use the previously successful pinned Node 24.21.0 toolchain consistently for frontend clean install, tests and build. Add/document the repository's chosen host version selector and compatible package engine constraints without claiming Node 26 is unsupported. The review's Node 26 exit 134 does not contradict an earlier successful Node 24 build; record exact versions and results. Do not launch a separate root-cause investigation once the chosen toolchain passes.

Use Java 21 and actual existing test tasks. Do not invent lint/Spotless success when no task exists. Run full backend/frontend suites and builds after repairs, and run packaged/native HTTP flows on a fresh and disposable upgraded database. Native smoke flows must actually create a PAPER account and execute a legacy manual trade using the real client's contract.

If browser execution is available natively, run the relevant flows. Keep unexecuted browser/container checks explicitly NOT VERIFIED. Use one unique Compose project/port and project-owned volume for all commands in any container run; inspect mappings before use. Do not reuse the completion report's inconsistent build/run project sequence or leave out the actual Playwright command. The M2 scope remains historical import/data inspection; the backtest engine belongs to M3.

Write `planning/reports/research-M1b-fixes.md` and update the old completion report's status/link so it cannot remain an unqualified COMPLETE claim. Include:

1. Starting/final source identity including the reviewed and fixed uncommitted state.
2. A numbered disposition for **findings 1–13**: reproduction or current contrary evidence, fix, regression test and actual result. Do not close a finding solely because its test name sounds relevant.
3. Separate rows for actual production-configured API flows, chat recovery stages, real writer races, each atomicity failure point, schema rejection, conversion rollback, digest change and old-binary startup.
4. Exact commands/toolchains/test totals and remaining environmental gates.
5. Candidate-schema compatibility/migration handling and evidence that the owner's database was untouched.
6. Remaining limitations and a justified M1b readiness assessment. Unproven native accounting/recovery requirements remain blockers for M2; inherited unavailable Docker execution remains a separate deployment gate.

Complete all feasible corrections and required native verification, then stop before M2. Return the fixes report and a concise summary of actual outcomes.
